import Link from "next/link";
import { redirect } from "next/navigation";
import { BadgeCheck, LayoutDashboard, Settings2, UserCog, Edit3 } from "lucide-react";
import { createClient } from "@/lib/supabase/server";
import styles from "./account.module.css";
export const dynamic="force-dynamic";
type Locale="ar"|"en";
type Profile={display_name:string|null;username:string|null;avatar_url:string|null;bio:string|null;is_verified:boolean|null;is_creator:boolean|null;trailer_url:string|null};
export default async function AccountPage({params}:{params:Promise<{locale:string}>}){const{locale:raw}=await params;const locale:Locale=raw==="en"?"en":"ar",ar=locale==="ar",s=await createClient(),{data:auth}=await s.auth.getUser();if(!auth.user)redirect(`/${locale}/auth?next=/${locale}/account`);const[{data:profileData},{count:savedCount},{count:followingCount}]=await Promise.all([s.from("profiles").select("display_name,username,avatar_url,bio,is_verified,is_creator,trailer_url").eq("id",auth.user.id).maybeSingle(),s.from("video_saves").select("id",{count:"exact",head:true}).eq("user_id",auth.user.id),s.from("follows").select("id",{count:"exact",head:true}).eq("follower_id",auth.user.id)]);const profile=profileData as Profile|null,name=profile?.display_name||auth.user.email?.split("@")[0]||(ar?"مستخدم RAVINE":"RAVINE user");return <section className={`section account-page ${styles.page}`} dir={ar?"rtl":"ltr"}><div className={styles.hero}><div><div className={styles.eyebrow}>RAVINE / {ar?"الحساب":"ACCOUNT"}</div><h1 className={styles.heroTitle}>{name}</h1><p className={styles.heroNote}>{profile?.username?`@${profile.username}`:""}{profile?.bio?` · ${profile.bio}`:""}</p></div><div className={styles.profileCard}><div className={styles.avatar}>{profile?.avatar_url?<img src={profile.avatar_url} alt=""/>:<span>{name.slice(0,1).toUpperCase()}</span>}</div><div className={styles.profileCopy}><strong>{profile?.is_creator?(ar?"مبدع":"Creator"):(ar?"مشاهد":"Viewer")}</strong><span>{profile?.is_verified?(ar?"حساب موثق":"Verified account"):(ar?"حساب شخصي":"Personal account")}</span></div></div></div><div className={styles.summaryGrid}><div className={styles.summaryCard}><span className={styles.summaryLabel}>{ar?"الحالة":"Status"}</span><strong className={styles.summaryValue}>{profile?.is_creator?(ar?"مبدع":"Creator"):(ar?"مشاهد":"Viewer")}</strong></div><div className={styles.summaryCard}><span className={styles.summaryLabel}>{ar?"المحفوظات":"Saved"}</span><strong className={styles.summaryValue}>{Number(savedCount??0).toLocaleString(locale)}</strong><span className={styles.summaryMeta}>{ar?"أعمال في مكتبتك":"Works in your library"}</span></div><div className={styles.summaryCard}><span className={styles.summaryLabel}>{ar?"المتابَعون":"Following"}</span><strong className={styles.summaryValue}>{Number(followingCount??0).toLocaleString(locale)}</strong><span className={styles.summaryMeta}>{ar?"مبدعون في مسارك":"Creators in your path"}</span></div></div><div className={styles.accountActionGrid}><Link className={styles.accountActionCard} href={`/${locale}/account/edit`}><span className={styles.accountActionIcon}><Edit3 size={18}/></span><span><strong>{ar?"تعديل الحساب":"Edit account"}</strong><small>{ar?"عدّل بياناتك وصورك وتريلر المبدع.":"Edit your details, media, and creator trailer."}</small></span></Link><Link className={styles.accountActionCard} href={`/${locale}/settings`}><span className={styles.accountActionIcon}><Settings2 size={18}/></span><span><strong>{ar?"الإعدادات العامة":"General settings"}</strong><small>{ar?"المظهر واللغة وتفضيلات التجربة":"Appearance, language, and experience preferences"}</small></span></Link>{profile?.is_creator&&profile.trailer_url?<a className={styles.accountActionCard} href={profile.trailer_url} target="_blank" rel="noreferrer"><span className={styles.accountActionIcon}>▶</span><span><strong>{ar?"تريلر القناة":"Channel trailer"}</strong><small>{ar?"مشاهدة التريلر":"Watch the channel trailer"}</small></span></a>:null}</div><div className={styles.actions}><Link className={styles.actionLink} href={`/${locale}/dashboard`}><LayoutDashboard size={14}/>{ar?"لوحة المستخدم":"User dashboard"}</Link>{profile?.is_verified?<span className={styles.actionLink}><BadgeCheck size={14}/>{ar?"موثق":"Verified"}</span>:null}</div></section>}
